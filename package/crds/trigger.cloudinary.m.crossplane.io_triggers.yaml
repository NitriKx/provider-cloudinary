---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: triggers.trigger.cloudinary.m.crossplane.io
spec:
  group: trigger.cloudinary.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - cloudinary
    kind: Trigger
    listKind: TriggerList
    plural: triggers
    singular: trigger
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Trigger is the Schema for the Triggers API. Manages a Cloudinary
          webhook notification trigger. Triggers fire on specific asset events and
          POST a payload to a configured URL. Up to 30 triggers per product environment
          (the console UI shows only 10).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TriggerSpec defines the desired state of Trigger
            properties:
              forProvider:
                properties:
                  additive:
                    description: |-
                      asset notification_url. Default false.
                      When true, this global trigger fires in addition to any per-asset notification_url. Default false.
                    type: boolean
                  authScheme:
                    description: |-
                      (String) Signature method for verifying incoming webhook payloads. One of: default, legacy_hmac, eddsa_v2.
                      Signature method for verifying incoming webhook payloads. One of: default, legacy_hmac, eddsa_v2.
                    type: string
                  eventType:
                    description: |-
                      (String) The event that fires the trigger. One of: upload, delete, rename, move, eager, explode, multi, resource_tags_changed, resource_context_changed, resource_metadata_changed, resource_display_name_changed, access_control_changed, related_assets, create_folder, delete_folder, move_or_rename_asset_folder, proof_status_changed, error, all.
                      The event that fires the trigger. One of: upload, delete, rename, move, eager, explode, multi, resource_tags_changed, resource_context_changed, resource_metadata_changed, resource_display_name_changed, access_control_changed, related_assets, create_folder, delete_folder, move_or_rename_asset_folder, proof_status_changed, error, all.
                    type: string
                  filter:
                    description: |-
                      encoded) that filters which events fire this trigger. When set, filter_language is automatically sent as "jsonlogic". Example: "{"==":[{"var":"resource_type"},"image"]}"
                      A JSONLogic expression (JSON-encoded) that filters which events fire this trigger. When set, filter_language is automatically sent as "jsonlogic". Example: "{\"==\":[{\"var\":\"resource_type\"},\"image\"]}"
                    type: string
                  payloadTemplate:
                    description: |-
                      encoded, max 16KB) that customises the notification payload. Available namespaces: event, asset, folder, eager, error, raw.
                      A Mustache template object (JSON-encoded, max 16KB) that customises the notification payload. Available namespaces: event, asset, folder, eager, error, raw.
                    type: string
                  uri:
                    description: |-
                      (String) The webhook URL to which Cloudinary sends the notification payload.
                      The webhook URL to which Cloudinary sends the notification payload.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  additive:
                    description: |-
                      asset notification_url. Default false.
                      When true, this global trigger fires in addition to any per-asset notification_url. Default false.
                    type: boolean
                  authScheme:
                    description: |-
                      (String) Signature method for verifying incoming webhook payloads. One of: default, legacy_hmac, eddsa_v2.
                      Signature method for verifying incoming webhook payloads. One of: default, legacy_hmac, eddsa_v2.
                    type: string
                  eventType:
                    description: |-
                      (String) The event that fires the trigger. One of: upload, delete, rename, move, eager, explode, multi, resource_tags_changed, resource_context_changed, resource_metadata_changed, resource_display_name_changed, access_control_changed, related_assets, create_folder, delete_folder, move_or_rename_asset_folder, proof_status_changed, error, all.
                      The event that fires the trigger. One of: upload, delete, rename, move, eager, explode, multi, resource_tags_changed, resource_context_changed, resource_metadata_changed, resource_display_name_changed, access_control_changed, related_assets, create_folder, delete_folder, move_or_rename_asset_folder, proof_status_changed, error, all.
                    type: string
                  filter:
                    description: |-
                      encoded) that filters which events fire this trigger. When set, filter_language is automatically sent as "jsonlogic". Example: "{"==":[{"var":"resource_type"},"image"]}"
                      A JSONLogic expression (JSON-encoded) that filters which events fire this trigger. When set, filter_language is automatically sent as "jsonlogic". Example: "{\"==\":[{\"var\":\"resource_type\"},\"image\"]}"
                    type: string
                  payloadTemplate:
                    description: |-
                      encoded, max 16KB) that customises the notification payload. Available namespaces: event, asset, folder, eager, error, raw.
                      A Mustache template object (JSON-encoded, max 16KB) that customises the notification payload. Available namespaces: event, asset, folder, eager, error, raw.
                    type: string
                  uri:
                    description: |-
                      (String) The webhook URL to which Cloudinary sends the notification payload.
                      The webhook URL to which Cloudinary sends the notification payload.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.eventType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.eventType)
                || (has(self.initProvider) && has(self.initProvider.eventType))'
            - message: spec.forProvider.uri is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.uri)
                || (has(self.initProvider) && has(self.initProvider.uri))'
          status:
            description: TriggerStatus defines the observed state of Trigger.
            properties:
              atProvider:
                properties:
                  additive:
                    description: |-
                      asset notification_url. Default false.
                      When true, this global trigger fires in addition to any per-asset notification_url. Default false.
                    type: boolean
                  authScheme:
                    description: |-
                      (String) Signature method for verifying incoming webhook payloads. One of: default, legacy_hmac, eddsa_v2.
                      Signature method for verifying incoming webhook payloads. One of: default, legacy_hmac, eddsa_v2.
                    type: string
                  createdAt:
                    description: |-
                      (String) ISO 8601 timestamp of when the trigger was created.
                      ISO 8601 timestamp of when the trigger was created.
                    type: string
                  eventType:
                    description: |-
                      (String) The event that fires the trigger. One of: upload, delete, rename, move, eager, explode, multi, resource_tags_changed, resource_context_changed, resource_metadata_changed, resource_display_name_changed, access_control_changed, related_assets, create_folder, delete_folder, move_or_rename_asset_folder, proof_status_changed, error, all.
                      The event that fires the trigger. One of: upload, delete, rename, move, eager, explode, multi, resource_tags_changed, resource_context_changed, resource_metadata_changed, resource_display_name_changed, access_control_changed, related_assets, create_folder, delete_folder, move_or_rename_asset_folder, proof_status_changed, error, all.
                    type: string
                  filter:
                    description: |-
                      encoded) that filters which events fire this trigger. When set, filter_language is automatically sent as "jsonlogic". Example: "{"==":[{"var":"resource_type"},"image"]}"
                      A JSONLogic expression (JSON-encoded) that filters which events fire this trigger. When set, filter_language is automatically sent as "jsonlogic". Example: "{\"==\":[{\"var\":\"resource_type\"},\"image\"]}"
                    type: string
                  id:
                    description: (String) The unique identifier of the trigger assigned
                      by Cloudinary.
                    type: string
                  payloadTemplate:
                    description: |-
                      encoded, max 16KB) that customises the notification payload. Available namespaces: event, asset, folder, eager, error, raw.
                      A Mustache template object (JSON-encoded, max 16KB) that customises the notification payload. Available namespaces: event, asset, folder, eager, error, raw.
                    type: string
                  productEnvironmentId:
                    description: |-
                      (String) The product environment this trigger is scoped to.
                      The product environment this trigger is scoped to.
                    type: string
                  updatedAt:
                    description: |-
                      (String) ISO 8601 timestamp of when the trigger was last updated.
                      ISO 8601 timestamp of when the trigger was last updated.
                    type: string
                  uri:
                    description: |-
                      (String) The webhook URL to which Cloudinary sends the notification payload.
                      The webhook URL to which Cloudinary sends the notification payload.
                    type: string
                  uriType:
                    description: |-
                      (String) The type of the URI. Always "webhook".
                      The type of the URI. Always "webhook".
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
